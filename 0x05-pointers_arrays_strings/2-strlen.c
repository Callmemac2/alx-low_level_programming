#include "main.h"

/**
 * _strlen - Function that return the length of a string.
 *
 * @s: thr input
 *
 * Return: Always 0.
 *
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; *s != '\0'; i++)
	{
		continue;
	}
	return (i);
}
