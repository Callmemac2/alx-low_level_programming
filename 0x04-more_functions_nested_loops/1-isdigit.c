#include "main.h"
#include <stdio.h>

/**
 * _isdigit - Function that checks for a digit(0 though 9)
 * @c: checks for digit
 * Return: 1 if is digit, 0 otherwise
 */

int _isdigit(int c)
{

	int i;

	for (i = '0'; i <= '9'; i++)
	{
		return (1);
	}

	return (0);
}
